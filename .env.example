# 백엔드 연동 시 사용. 실제 값은 .env 파일에 넣으세요 (.env 는 git 에 올리지 않음).

# 실제 백엔드 API 베이스 URL (통합 설계서 §4: https://{host}/api/v1)
VITE_API_BASE_URL=http://localhost:8080/api

# true 로 두면 실제 백엔드(realApi) 사용, 비우거나 false 면 mockApi 사용
VITE_USE_REAL_API=false
